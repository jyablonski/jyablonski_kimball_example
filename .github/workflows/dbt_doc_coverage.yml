name: dbt Doc Coverage

on: [pull_request]

env:
  S3_BUCKET: ${{ secrets.S3_BUCKET }}
  IAM_ROLE: ${{ secrets.IAM_ROLE }}
  AWS_REGION: "us-east-1"
  POETRY_VERSION: "1.8.3"
  PYTHON_VERSION: "3.11"

permissions:
  id-token: write
  contents: read
  pull-requests: write

jobs:
  dbt_coverage:
    name: dbt Doc Coverage
    runs-on: ubuntu-20.04

    steps:
      - name: Checkout the Repo
        uses: actions/checkout@v4

      - name: Install Poetry
        run: |
          python -m pip install --upgrade pip
          pip install poetry==${{ env.POETRY_VERSION }}

      - name: Install Python
        uses: actions/setup-python@v4
        id: setup_python
        with:
          python-version: ${{ env.PYTHON_VERSION }}
          cache: poetry
          cache-dependency-path: poetry.lock

      - name: Install dependencies
        run: |
          poetry install

      - name: Configure Aws Credentials
        uses: aws-actions/configure-aws-credentials@v4
        with:
          role-to-assume: ${{ env.IAM_ROLE }}
          role-session-name: kimball-dbt-doc-coverage
          aws-region: ${{ env.AWS_REGION }}

      - name: Get Coverage File
        run: |
          aws s3 cp s3://${{ env.S3_BUCKET }}/kimball/coverage-prod.json coverage-prod.json

      - name: Print Coverage
        run: |
          make compare-coverage | tee coverage_output.txt

      - name: Extract and Post Coverage Summary
        run: |
          # Extract the part of the output starting with the section containing "before after +/-"
          coverage_summary=$(awk '/before *after *\+\-/{f=1} f' coverage_output.txt)

          # Format it as a comment for the PR
          echo "## dbt Doc Coverage Summary" > comment.txt
          echo "\`\`\`" >> comment.txt
          echo "$coverage_summary" >> comment.txt
          echo "\`\`\`" >> comment.txt

          # Post the comment to the PR
          gh pr comment ${{ github.event.pull_request.number }} --body-file comment.txt

        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
