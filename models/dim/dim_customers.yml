version: 2

models:
  - name: dim_customers
    columns:
      - name: customer_id
        data_tests:
          - not_null
          - relationships: # dbterd looks for all kind of this test
              to: ref('customer_integration_history_scd2')
              field: customer_id