version: 2

models:
    - name: paid_invoices
      columns:
          - name: order_id
            tests:
              - unique
          - name: invoice_id
            tests:
              - unique

